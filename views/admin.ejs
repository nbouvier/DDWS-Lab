<!DOCTYPE html>
<html>

<%- include('../partials/head'); %>

<body>
    <%- include('../partials/sideBar'); %>

    <div id="content">
        <%- include('../partials/header'); %>

        <h2>Users</h2>

        <table id="adminUserList" class="table">
            <thead>
                <tr>
                    <th scope="col">Email</th>
                    <th scope="col">First Name</th>
                    <th scope="col">Last Name</th>
                    <th scope="col">Status</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>

        <div id="blockUserModal" class="modal fade" tabindex="-1">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                              <i class="fas fa-times"></i>
                        </button>
                    </div>
                    <div class="modal-body">
                        <p>
                            How much time do you want the user
                            <span id="blockUserFirstName" class="font-weight-bold"></span>
                            <span id="blockUserLastName" class="font-weight-bold"></span>
                            to be block from selling his production to the market ?
                        </p>
                        <form id="blockUserForm" method="post" action="/api/user/block">
                            <input type="text" class="d-none" id="blockUserID">
                            <div class="form-row">
                                <div class="form-group col-md-6">
                                    <input type="number" class="form-control" id="blockUserTime" value="1">
                                </div>
                                <div class="form-group col-md-6">
                                    <select class="custom-select mr-sm-2" id="blockUserTimeUnit">
                                        <option value="m" selected>minute(s)</option>
                                        <option value="h">hour(s)</option>
                                        <option value="d">day(s)</option>
                                        <option value="w">week(s)</option>
                                    </select>
                                </div>
                            </div>
                            <div class="text-right">
                                <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger">Block</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div id="deleteUserModal" class="modal fade" tabindex="-1">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        ${header}
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                              <i class="fas fa-times"></i>
                        </button>
                    </div>
                    <div class="modal-body">
                        <p>
                            Do you really want to delete the user
                            <span id="deleteUserFirstName" class="font-weight-bold"></span>
                            <span id="deleteUserLastName" class="font-weight-bold"></span> ?
                        </p>
                        <form id="deleteUserForm" method="post" action="/api/user/delete">
                            <input type="text" class="d-none" id="deleteUserID">
                            <div class="text-right">
                                <button type="button" class="btn btn-danger" data-dismiss="modal">Cancel</button>
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../partials/footer'); %>
    </div>

    <%- include('../partials/scripts', { view: 'admin' }); %>
</body>

</html>
